let mergeState={column:'',allColumns:[],currentColumnIdx:0,letterValues:{},currentLetter:'',pendingMerges:[],mergedCount:0};function getTextColumns(){const lines=cleanedCSV.trim().split('\n');const headers=lines[0].split(',').map(h=>h.trim());const textCols=[];for(let col=0;col<headers.length;col++){let isText=false;for(let row=1;row<Math.min(6,lines.length);row++){const cell=lines[row].split(',')[col];if(cell&&isNaN(cell)){isText=true;break;}}if(isText)textCols.push(headers[col]);}return textCols;}function parseCSVLine(line){const cells=[];let current='';let inQuotes=false;for(let i=0;i<line.length;i++){const c=line[i];if(c==='"'){inQuotes=!inQuotes;}else if(c===','&&!inQuotes){cells.push(current.trim());current='';}else{current+=c;}}cells.push(current.trim());return cells;}async function startMergeGame(){document.getElementById('mergeGameSection').style.display='block';mergeState.allColumns=getTextColumns();mergeState.currentColumnIdx=0;if(mergeState.allColumns.length===0){autoDownloadCSV();return;}loadNextColumn();}async function loadNextColumn(){if(mergeState.currentColumnIdx>=mergeState.allColumns.length){showMergeSummary();return;}mergeState.column=mergeState.allColumns[mergeState.currentColumnIdx];mergeState.pendingMerges=[];loadUniqueValuesForColumn();advanceToNextLetterWithValues();}function loadUniqueValuesForColumn(){const lines=cleanedCSV.trim().split('\n');const headers=parseCSVLine(lines[0]);const colIdx=headers.indexOf(mergeState.column);if(colIdx===-1)return;const freq={};for(let i=1;i<lines.length;i++){const cells=parseCSVLine(lines[i]);const val=cells[colIdx];if(val)freq[val]=(freq[val]||0)+1;}mergeState.letterValues={};for(const [val,count] of Object.entries(freq)){const letter=val[0].toUpperCase();if(!mergeState.letterValues[letter])mergeState.letterValues[letter]=[];mergeState.letterValues[letter].push({value:val,count:count,checked:false});}}function advanceToNextLetterWithValues(){const alphabet='ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('');for(const letter of alphabet){if(mergeState.letterValues[letter]&&mergeState.letterValues[letter].length>1){mergeState.currentLetter=letter;renderMergeInterface();return;}}mergeState.currentColumnIdx++;loadNextColumn();}function renderMergeInterface(){const board=document.getElementById('gameBoard');board.innerHTML='';const container=document.createElement('div');container.style.cssText='padding:20px;';const header=document.createElement('div');header.style.cssText='margin-bottom:20px;';const letterDisplay=document.createElement('div');letterDisplay.style.cssText='font-size:28px;font-weight:bold;color:#333;';letterDisplay.textContent=mergeState.currentLetter;const columnDisplay=document.createElement('div');columnDisplay.style.cssText='font-size:12px;color:#666;margin-top:4px;';columnDisplay.textContent=`column: ${mergeState.column}`;header.appendChild(letterDisplay);header.appendChild(columnDisplay);container.appendChild(header);const values=mergeState.letterValues[mergeState.currentLetter]||[];const listDiv=document.createElement('div');listDiv.style.cssText='margin-bottom:20px;border:1px solid #ddd;border-radius:4px;padding:12px;background:#fafafa;max-height:400px;overflow-y:auto;';for(let i=0;i<values.length;i++){const item=values[i];const itemDiv=document.createElement('div');itemDiv.style.cssText='display:flex;align-items:center;gap:8px;padding:6px;margin-bottom:4px;';const checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.checked=item.checked;checkbox.onchange=(e)=>{item.checked=e.target.checked;};itemDiv.appendChild(checkbox);const label=document.createElement('label');label.style.cssText='flex:1;font-size:13px;cursor:pointer;';label.textContent=`${item.value} (${item.count})`;label.onclick=()=>{checkbox.checked=!checkbox.checked;item.checked=checkbox.checked;};itemDiv.appendChild(label);listDiv.appendChild(itemDiv);}container.appendChild(listDiv);const controlsDiv=document.createElement('div');controlsDiv.style.cssText='margin-bottom:16px;display:flex;gap:8px;align-items:center;';const canonicalLabel=document.createElement('label');canonicalLabel.style.cssText='font-size:12px;font-weight:bold;';canonicalLabel.textContent='merge to:';const canonicalInput=document.createElement('input');canonicalInput.type='text';canonicalInput.style.cssText='flex:1;padding:6px;border:1px solid #ddd;font-size:13px;';canonicalInput.placeholder='enter canonical value';controlsDiv.appendChild(canonicalLabel);controlsDiv.appendChild(canonicalInput);container.appendChild(controlsDiv);const mergeBtn=document.createElement('button');mergeBtn.style.cssText='padding:8px 16px;background:#0000ee;color:#fff;border:none;cursor:pointer;font-size:13px;margin-bottom:20px;';mergeBtn.textContent='add merge group';mergeBtn.onclick=()=>{const selected=values.filter(v=>v.checked);if(selected.length<2){alert('select at least 2 values to merge');return;}const canonical=canonicalInput.value.trim();if(!canonical){alert('enter a canonical value');return;}mergeState.pendingMerges.push({values:selected.map(v=>v.value),mergeInto:canonical});for(const v of selected)v.checked=false;canonicalInput.value='';canonicalInput.focus();renderMergeInterface();};container.appendChild(mergeBtn);const nextLetterBtn=document.createElement('button');nextLetterBtn.style.cssText='padding:8px 16px;background:#333;color:#fff;border:none;cursor:pointer;font-size:13px;';nextLetterBtn.textContent='next letter';nextLetterBtn.onclick=async()=>{if(mergeState.pendingMerges.length>0){await applyMergesForLetter();}advanceToNextLetterWithValues();};container.appendChild(nextLetterBtn);board.appendChild(container);}async function applyMergesForLetter(){const payload={csvData:cleanedCSV,column:mergeState.column,merges:mergeState.pendingMerges};try{const res=await fetch(window.location.origin+'/api/merge-clusters',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!res.ok){console.error('Merge failed with status',res.status);return;}const data=await res.json();if(data.csvData){cleanedCSV=data.csvData;mergeState.mergedCount+=mergeState.pendingMerges.length;mergeState.pendingMerges=[];loadUniqueValuesForColumn();}else{console.error('No csvData in response',data);}}catch(e){console.error('Merge error:',e);}}function showMergeSummary(){document.getElementById('mergeGameSection').style.display='none';document.getElementById('completeSection').style.display='block';const summaryDiv=document.createElement('div');summaryDiv.style.cssText='padding:20px;background:#f0f0f0;border-radius:4px;margin-bottom:20px;';summaryDiv.innerHTML=`<strong>merge complete:</strong> ${mergeState.mergedCount} merge groups applied across ${mergeState.allColumns.length} columns`;document.getElementById('gameBoard').appendChild(summaryDiv);updateTableDisplay(cleanedCSV.trim().split('\n')[0].split(','));displayAuditLog();}function autoDownloadCSV(){document.onkeydown=null;const csv=cleanedCSV;const blob=new Blob([csv],{type:'text/csv;charset=utf-8;'});const link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download=`cleaned_data_${new Date().toISOString().split('T')[0]}.csv`;link.click();setTimeout(()=>{document.getElementById('mergeGameSection').style.display='none';document.getElementById('completeSection').style.display='block';updateTableDisplay(cleanedCSV.trim().split('\n')[0].split(','));displayAuditLog();},500);}
