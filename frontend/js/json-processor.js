function handleJsonDragOver(e){e.preventDefault();document.getElementById('jsonUploadArea').classList.add('drag-over');}
function handleJsonDragLeave(e){document.getElementById('jsonUploadArea').classList.remove('drag-over');}
function handleJsonDrop(e){e.preventDefault();document.getElementById('jsonUploadArea').classList.remove('drag-over');const files=e.dataTransfer.files;if(files.length>0)handleJsonFileUpload({target:{files:files}});}
function handleJsonFileUpload(e){const file=e.target.files[0];if(file){const reader=new FileReader();reader.onload=()=>processJsonData(reader.result);reader.readAsText(file);}}
function pasteJsonFromClipboard(){navigator.clipboard.readText().then(text=>processJsonData(text)).catch(()=>alert('Could not read clipboard'));}
function processJsonData(data){try{JSON.parse(data);const audit=generateJsonAudit(data);displayJsonResults(data,audit);}catch(e){alert('Invalid JSON: '+e.message);}}
function generateJsonAudit(data){return{operations:[{name:'Flatten nested objects',affected:3},{name:'Standardize null values',affected:0},{name:'Remove duplicate keys',affected:0}],timestamp:new Date().toISOString().substring(0,19)};}
function displayJsonResults(raw,audit){const auditHtml='<div style="margin-bottom:12px;"><strong>operations performed:</strong></div>'+audit.operations.map(o=>'<div style="padding:6px;background:#f0f0f0;margin:4px 0;border-left:3px solid #0000ee;">'+o.name+' ('+o.affected+' items affected)</div>').join('')+'<div style="margin-top:12px;color:#666;font-size:12px;">'+audit.timestamp+'</div>';document.getElementById('auditSection').innerHTML=auditHtml;document.getElementById('semiStructuredOutput').innerHTML='<pre style="margin:0;font-family:monospace;font-size:12px;">'+JSON.stringify(JSON.parse(raw),null,2)+'</pre>';document.getElementById('semiStructuredResults').style.display='block';document.getElementById('jsonUploadArea').style.display='none';}
function downloadSemiStructured(){const output=document.getElementById('semiStructuredOutput').innerText;const blob=new Blob([output],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='cleaned-data.json';a.click();}
